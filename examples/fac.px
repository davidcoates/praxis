
fac : Int -> Int
fac 0 = 1
fac n = n * fac (n - 1)

main : () -> () # StdIn, StdOut
main () = putInt (fac (getInt ()))

-- Alternatives:
-- main = putInt . fac . getInt
-- main = let x = getInt() 
--         in putInt (fac x)
