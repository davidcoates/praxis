
operator (_ + _) = add


operator (_ - _) = subtract where
  associates left
  precedence equal (_ + _)

operator (_ * _) = multiply where
  associates left
  precedence above (_ + _)

-- FIXME, prefix (presumably postfix, closed (?) too) not working
-- operator (- _) = negate

-- 7
x = 1 + 2 * 3

-- -4
y = 1 - 2 - 3
